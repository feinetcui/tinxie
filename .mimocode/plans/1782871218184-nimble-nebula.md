# 前端优化调整计划

## 当前状态分析

### 已完成的优化
✅ 首页童趣设计（彩虹背景、可爱装饰、大字体）
✅ 语音播报功能
✅ 音效反馈系统
✅ 友好的提示语

### 需要调整的问题

#### 1. 控制端 (host.html) - 缺少童趣风格
- **问题**：host.css 样式还是旧版，没有应用新的童趣配色
- **影响**：控制端界面与首页风格不一致

#### 2. 倒计时默认值不一致
- **问题**：player.html 中 timer 默认显示 "10"，但 JS 设置为 15 秒
- **位置**：player.html 第 40 行

#### 3. 控制端计时设置默认值
- **问题**：host.html 中默认选中 10 秒，应改为 15 秒
- **位置**：host.html 第 62 行

#### 4. 手动输入提示
- **问题**：host.html 的手动输入区域缺少提示用户可以手动输入的说明

---

## 修改计划

### 修改 1: 更新 host.css 童趣风格
**文件**: `public/css/host.css`
**修改内容**:
- 更新房间号显示样式（更大的字体、渐变色）
- 更新按钮样式（圆角、渐变背景）
- 更新卡片样式（更大的圆角、阴影）
- 更新状态区域样式

### 修改 2: 修复倒计时默认值
**文件**: `public/player.html`
**修改内容**:
- 第 40 行：`<div class="timer" id="timer">10</div>` → `<div class="timer" id="timer">15</div>`

### 修改 3: 修复控制端默认计时
**文件**: `public/host.html`
**修改内容**:
- 第 62 行：移除 `active` 类，添加到 15 秒按钮
- 第 65 行：更新显示为 "15"

### 修改 4: 添加手动输入提示
**文件**: `public/host.html`
**修改内容**:
- 在手动输入区域添加提示文字

---

## 验证步骤
1. 打开首页检查样式一致性
2. 打开控制端检查房间号显示
3. 打开选手端检查倒计时默认值
4. 测试完整听写流程
